{{#each Articles}}
<article>
    <h2>{{Title}}</h2>
    <h3>{{Subtitle}}</h3>
    <div class='newcontent'>
        {{Content}}
    </div>
    <div>
        {{Author}} - on {{Date}}
    </div>
</article>
{{/each}}