import ContactMessagesManagementPage from "@/components/management/contact-messages-management-page";

/**
 * Renders the Contact Dashboard Page component and returns the UI used by the contact view.
 * @returns {React.ReactNode} Returns the rendered UI used by the contact view.
 */
export default function ContactDashboardPage() {
  return <ContactMessagesManagementPage />;
}
